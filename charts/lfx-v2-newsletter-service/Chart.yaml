# Copyright The Linux Foundation and each contributor to LFX.
# SPDX-License-Identifier: MIT
apiVersion: v2
name: lfx-v2-newsletter-service
description: LFX Newsletter Service — owns newsletter persistence and send orchestration
type: application
# This version should not be incremented, as it is dynamically replaced with the release version during the chart build
# job.
version: 0.0.1
appVersion: "latest"
